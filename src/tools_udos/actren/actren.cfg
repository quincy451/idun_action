FEATURES {
    STARTADDRESS: default = $0900;
}

MEMORY {
    ZP:   file = "", start = $00F7, size = $0009, type = rw, define = yes;
    LOAD: file = %O, start = %S,    size = $1000, type = rw, define = yes;
}

SEGMENTS {
    ZPTEMP: load = ZP,   type = zp;
    CODE:   load = LOAD, type = ro;
}
