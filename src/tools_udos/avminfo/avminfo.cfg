MEMORY {
  ZP:   start = $00EC, size = $0010, type = rw, define = yes;
  CODE: start = $0900, size = $1800, type = rw, define = yes;
}

SEGMENTS {
  ZPTEMP: load = ZP, type = zp;
  CODE:   load = CODE, type = rw;
}
