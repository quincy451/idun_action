MEMORY {
  ZPTEMP: start = $00E0, size = $0019, define = yes;
  LOAD:   start = $1800, size = $21C0, define = yes, file = %O;
}

SEGMENTS {
  STARTUP: load = LOAD;
  CODE:    load = LOAD;
  BSS:     load = LOAD, type = bss, define = yes;
  ZPTEMP:  load = ZPTEMP, type = zp;
}
